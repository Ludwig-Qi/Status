# G3 0C 实现模块说明

Stage 0C 可立即实现部分（纯代码 + 合成数据验证，不依赖真实数据）。

## 文件清单

| 文件 | 功能 | 状态 |
|------|------|------|
| `g3_utils.py` | 缩尾 / 标准化 / 缺失处理 / 公式 Hash / 固定管线 | ✅ 已测试 |
| `g3_pit_replay.py` | 美股→A股交易日映射 / PIT 回放 / 前视检测 / 批量回放 | ✅ 已测试 |
| `test_g3.py` | 25 个合成数据单元测试 | ✅ 全部通过 |
| `repo/G3_0C_feature_builder.py` | 13 项 B 类特征构造（公式 + snapshot 落表） | ⚠️ 待真实数据 |

## 依赖关系

```
test_g3.py
  ├── g3_utils.py      （无外部依赖）
  └── g3_pit_replay.py （无外部依赖）

G3_0C_feature_builder.py（repo 中）
  ├── g3_utils.py      （fix_pipeline 接入后使用）
  └── g3_pit_replay.py （replay / validate_no_future_shift 接入后使用）
```

## 关键函数调用约定

### g3_utils.py

```python
from g3_utils import fix_pipeline, compute_formula_hash

# 特征处理固定顺序：raw → ffill → winsorize → zscore
clean = fix_pipeline(raw_df, winsor_lower=0.01, winsor_upper=0.99,
                     normalize="zscore", missing="ffill")

# 公式 Hash → snapshot 的 feature_version 字段
h = compute_formula_hash("SPY.Ret", version="v1.0")  # 16位hex
```

### g3_pit_replay.py

```python
from g3_pit_replay import replay, run_replay_suite, map_us_to_ashare_day

# 单日回放
r = replay("2024-03-15", builder_fn)
print(r.passed)          # 是否通过无前视校验
print(r.dropped_future)  # 被丢弃的未来数据行数

# 批量回放
report = run_replay_suite(["2024-01-05", "2024-01-08"], builder_fn)

# 美股→A股映射（需真实日历）
mapped = map_us_to_ashare_day(us_dates, ashare_calendar)
```

## 待数据到位后的接入步骤

1. **配置 DATA_ROOT**：`export COS_DATA_ROOT=/实际/镜像/路径`
2. **校准 DATA_LAYOUT**：确认 `repo/G3_0C_feature_builder.py` 中的路径字典与实际目录一致
3. **验证 ETF 覆盖**：运行 `verify_cos_coverage()`，确认 14 个 Ticker 存在
4. **构造 13 项特征**：运行 `build_all_b_features()`
5. **接入清洗管线**：`fix_pipeline` 应用于构造结果
6. **PIT 回放**：用真实 `us_calendar` / `ashare_calendar` 跑 `run_replay_suite`
7. **落表**：`build_snapshot_table()` → Parquet
